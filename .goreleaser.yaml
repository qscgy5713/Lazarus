version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: lazarus
    main: ./cmd/lazarus
    binary: lazarus
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: lazarus
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE
      - lazarus.example.yml

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^test:"
      - "^chore:"

release:
  # goreleaser creates the GitHub Release itself from the pushed tag; this
  # repo has no separate changelog file to keep in sync.
  github:
    owner: qscgy5713
    name: Lazarus
